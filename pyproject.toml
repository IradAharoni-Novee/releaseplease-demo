[project]
name = "releaseplease-demo"
version = "0.3.0"
description = "Demo repository for release-please-driven environment promotion"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
